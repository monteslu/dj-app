/**
 * Recorder worklet entry — bundled separately by Vite, loaded into the
 * AudioWorkletGlobalScope. Pulls in the recorder worklet (registerProcessor side
 * effect).
 */
import '@internal-dj/codec/recorder-worklet';
