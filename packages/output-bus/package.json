{
  "name": "@dj/output-bus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Data-emission bus: dj-app publishes master-bus audio + track metadata + per-display visualization directives over a PLUGGABLE transport (worker/postMessage, WebSocket, RTC, …). dj-app renders nothing — displays consume the data and visualize. Web + Electron.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc --build", "test": "vitest run" }
}
